multibranchPipelineJob('kuber') {
    branchSources {
        github {
            id('kuber') // IMPORTANT: use a constant and unique identifier
            scanCredentialsId('github-username-access-token')
            repoOwner('t0ster')
            repository('kuber-ui')
        }
    }
}
